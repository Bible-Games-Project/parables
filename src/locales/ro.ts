import { en } from "@/locales/en";
import type { LocaleDictionary } from "@/locales/en";

// TODO: translate to Romanian. All values are English placeholders for now,
// which keeps the localization architecture complete while translation work
// happens separately.
export const ro: LocaleDictionary = { ...en };
